# Shuai's truffle project

> This project uses `truffle` and `vue.js` as the technology stack to build a simple crowdfunding, try crowdfunding master topic hardware.

## basic run

1. yarn install
1. yarn serve
